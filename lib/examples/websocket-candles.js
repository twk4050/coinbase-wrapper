// import { WebSocketChannel } from 'src/types/websocket';
// import { Event, WebSocketClient } from 'src/websocket-client';
// const key = process.env.key || '';
// const secret = process.env.secret || '';
// const client = new WebSocketClient(key, secret);
// client.on(Event.MESSAGE_CANDLES, (candlesMessage) => {
//     const candles = candlesMessage.events[0].candles;
//     console.log(candlesMessage.channel);
//     console.log(candles);
// });
// client.connect();
// client.subscribe(WebSocketChannel.CANDLES, ['ETH-USDC']);
//# sourceMappingURL=websocket-candles.js.map